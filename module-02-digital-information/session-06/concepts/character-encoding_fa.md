# کدگذاری کاراکتر: چگونه کامپیوترها حروف را ذخیره می‌کنند

## مقدمه: چالش متن در دنیای اعداد

کامپیوترها فقط اعداد را می‌فهمند. فقط اعداد! اما وقتی این جمله را می‌خوانید:
```
سلام دنیا
```

کامپیوتر آن را به صورت اعداد می‌بیند:
```
سلام = [1587, 1604, 1575, 1605]  (نقاط کد فارسی در UTF-8)
```

**کدگذاری کاراکتر** فرآیند تبدیل کاراکترهای انسان‌خواندنی (حروف، اعداد، نمادها) به اعداد کامپیوترخواندنی است.

---

## چرا به کدگذاری نیاز داریم؟

### تصور کنید: ایجاد یک "زبان" کامپیوتر

بیایید بگوییم می‌خواهید از کامپیوتر برای نوشتن استفاده کنید. باید:
۱. **تمام کاراکترهای ممکن** را فهرست کنید (حروف، اعداد، علائم نگارشی)
۲. **به هر یک یک شماره منحصر به فرد** بدهید
۳. **قرارداد** را به همه بگویید

این یک **مجموعه کاراکتر** یا **کدگذاری** است!

### مثال ساده: یک کدگذاری ۳ حرفی

```
ایجاد کدگذاری "بچه‌گانه":

شماره → کاراکتر
   ۱  →  "A"
   ۲  →  "B"  
   ۳  →  "C"

حالا پیام بنویسید:
"ABC" = ۱, ۲, ۳

برای خواندن، فقط اعداد را به حروف برگردانید!
```

**مشکل واقعی**: دنیا به بیش از ۳ حرف نیاز دارد!

---

## ASCII: اولین کدگذاری استاندارد (۱۹۶۳)

### تاریخچه کوتاه ASCII

**ASCII** (American Standard Code for Information Interchange) اولین کدگذاری مهم بود:
- ساخته شد: ۱۹۶۰
- هدف: استاندارد کردن ارتباط بین کامپیوترهای اولیه
- طول: ۷ بیت = ۱۲۸ کاراکتر (۰-۱۲۷)

### جدول ASCII

| محدوده | کاربرد | مثال‌ها |
|--------|--------|---------|
| ۰-۳۱ | کنترل (غیرقابل چاپ) | Enter (۱۳)، Tab (۹)، Escape (۲۷) |
| ۳۲ | Space | فاصله |
| ۴۸-۵۷ | اعداد | ۰، ۱، ۲، ...، ۹ |
| ۶۵-۹۰ | حروف بزرگ | A، B، C، ...، Z |
| ۹۷-۱۲۲ | حروف کوچک | a، b، c، ...، z |
| ۳۳-۴۷ | علائم نگارشی | ! " # $ % & ' ( ) * + , - . / |

### ASCII در عمل

```
کاراکتر    دهدهی    دودویی
   "A"  →   ۶۵   →  01000001
   "a"  →   ۹۷   →  01100001
   "0"  →   ۴۸   →  00110000
   " "  →   ۳۲   →  00100000
```

**نکته مهم**: "A" (۶۵) و "a" (۹۷) متفاوت هستند! ۳۲ = تفاوت بین بزرگ و کوچک.

### جادوی ASCII: فضای زیاد نیست

همه ASCII در ۷ بیت جا می‌شود:
```
حداکثر: 1111111 = ۱۲۷
بنابراین: ۱۲۸ کاراکتر مختلف ممکن است (۰-۱۲۷)
```

**مشکل**: ۱۲۸ کاراکتر برای زبان‌های غیرانگلیسی کافی نیست!

---

## ASCII توسعه‌یافته: تلاش ۱

### ایده: از بیت ۸ ام استفاده کنید

ASCII فقط ۷ بیت استفاده می‌کرد، بنابراین افراد شروع به استفاده از بیت ۸ ام کردند:
```
۷ بیت: ۱۲۸ کاراکتر
۸ بیت: ۲۵۶ کاراکتر (دو برابر!)
```

### مشکل: استانداردهای متعدد ASCII توسعه‌یافته

کشورهای مختلف بیت اضافی را متفاوت استفاده کردند:
- **ISO-8859-1** (غرب اروپا): ñ، é، ü
- **ISO-8859-5** (سیریلیک): русские буквы
- **Windows-1252** (توسعه‌یافته ASCII مایکروسافت)

**نتیجه**: اسناد ناسازگار! یک فایل در کامپیوتر فرانسه خوب به نظر می‌رسید اما در کامپیوتر روسی ناخوانا.

---

## یونیکد: راه‌حل یکپارچه (۱۹۹۱)

### ایده بزرگ: یک فضای کد برای همه زبان‌ها

**یونیکد** به هر کاراکتر در هر زبان یک شماره منحصر به فرد می‌دهد:
- هدف: یک فضای کد واحد برای همه زبان‌های جهان
- فضای کد: ۰ تا 1,114,111 (بیش از ۱ میلیون!)
- شامل: ۱۴۳,۰۰۰+ کاراکتر تعریف‌شده، به همه زبان‌های زنده و بسیاری مرده

### نقاط کد یونیکد چگونه کار می‌کنند

```
نقطه کد یونیکد → کاراکتر
      U+0041     → "A"          (ASCII)
      U+00E9     → "é"          (فرانسوی)
      U+4E2D     → "中"          (چینی)
      U+1F600    → "😀"          (ایموجی)
      U+0627     → "ا"          (عربی)
      U+0634     → "ش"          (عربی)
```

**U+** یعنی "یونیکد پلاس" - روش نوشتن نقاط کد.

### بلوک‌های یونیکد (زبان‌ها و نمادها)

| بلوک | محدوده | شامل |
|------|--------|-------|
| ASCII/پایه لاتین | U+0000-U+007F | A-Z، a-z، ۰-۹، علائم نگارشی |
| لاتین-۱ مکمل | U+0080-U+00FF | é، ñ، ç، © |
| گسترش IPA | U+0250-U+02AF | نمادهای فونتیک |
| یونانی | U+0370-U+03FF | α، β، γ، Ω |
| سیریلیک | U+0400-U+04FF | русский |
| عربی | U+0600-U+06FF | العربية |
| فارسی | U+0600-U+06FF + الحاقات | فارسی |
| چینی/ژاپنی/کره‌ای | U+4E00-U+9FFF | 中日韩 |
| ایموجی | U+1F600-U+1F64F | 😀 😃 😄 |

---

## UTF-8: پیاده‌سازی یونیکد

### مشکل: چگونه یونیکد را ذخیره کنیم؟

یونیکد تا ۱ میلیون کاراکتر دارد:
- با ۱ بایت: فقط ۲۵۶ کاراکتر
- با ۲ بایت: ۶۵,۵۳۶ کاراکتر
- با ۳ بایت: ۱۶ میلیون کاراکتر

**راه‌حل**: از بایت‌های **متغیر** استفاده کنید!

### چگونه UTF-8 کار می‌کند

**UTF-8** (UCS Transformation Format - 8-bit) از ۱ تا ۴ بایت برای هر کاراکتر استفاده می‌کند:

| کاراکترها | بایت‌ها | الگوی باینری | مثال |
|-----------|---------|--------------|------|
| ASCII (۰-۱۲۷) | ۱ | 0xxxxxxx | "A" = 01000001 |
| لاتین، اروپایی (۱۲۸-۲۰۴۷) | ۲ | 110xxxxx 10xxxxxx | "é" = 11000011 10101001 |
| چینی، فارسی (۲۰۴۸-۶۵,۵۳۵) | ۳ | 1110xxxx 10xxxxxx 10xxxxxx | "ا" = 11000010 11000111 |
| ایموجی (۶۵,۵۳۶+) | ۴ | 11110xxx 10xxxxxx 10xxxxxx 10xxxxxx | "😀" = 11110000 10011111 10011000 10000000 |

### UTF-8 چرا بسیار محبوب است

**مزیت ۱: سازگاری به عقب با ASCII**
- فایل‌های ASCII خالص در UTF-8 به طور یکسان هستند
- برنامه‌های قدیمی می‌توانند UTF-8 را بخوانند (تا جایی که کاراکترهای غیرASCII نیست)

**مزیت ۲: کارآمد برای متن انگلیسی**
- اکثر محتوای وب انگلیسی است
- ASCII/انگلیسی در UTF-8 فقط ۱ بایت است
- صرفه‌جویی در فضا نسبت به UTF-16 (که همیشه حداقل ۲ بایت است)

**مزیت ۳: سازگاری بایت**
- هرجا که بایت ۰xxxxxxx (زیر ۱۲۸) ببینید، یک کاراکتر ASCII است
- هرجا بایت ۱xxxxxxx (۱۲۸ یا بالاتر) ببینید، بخشی از یک کاراکتر چندبایتی است

**نتیجه**: ۹۵%+ از وب از UTF-8 استفاده می‌کند!

---

## سایر رمزگذاری‌های یونیکد

### UTF-16

- **۲ یا ۴ بایت** در هر کاراکتر
- استفاده می‌شود: ویندوز داخلی، جاوا، JavaScript داخلی
- مزیت: دسترسی تصادفی سریع (هر کاراکتر همان اندازه یا دو برابر)
- معایب: نیاز به مدیریت "جفت‌های جایگزین" برای ایموجی‌ها

```
"A" در UTF-16:  0000 0000 0100 0001  (۲ بایت)
"中" در UTF-16:  0100 1110 0010 1101  (۲ بایت)
"😀" در UTF-16:  D83D DE00  (۴ بایت، جفت جایگزین)
```

### UTF-32

- **همیشه ۴ بایت** در هر کاراکتر
- ساده‌ترین برای پردازش
- هدررفت حافظه (متن ASCII ۴ برابر UTF-8 است)
- استفاده نادر در ذخیره‌سازی، اما گاهی در پردازش داخلی

---

## مشکل BOM (نشانگر ترتیب بایت)

### BOM چیست؟

**BOM** (Byte Order Mark) یک کاراکتر خاص است که به کامپیوتر می‌گوید:
- این متن یونیکد است
- بایت‌ها چگونه مرتب شده‌اند (Big Endian یا Little Endian)

### BOM در UTF-8
```
BOM = EF BB BF (سه بایت در ابتدای فایل)
```

مشکل: برخی برنامه‌ها BOM را نشان می‌دهند (به نظر می‌رسد "ï»¿" در ابتدای فایل)!

### BOM در UTF-16
```
Big Endian:    FE FF
Little Endian: FF FE
```

---

## مشکلات کدگذاری رایج

### مشکل ۱: علائم سوال یا جعبه‌های خالی

**علامت:** کاراکترهایی که نباید آنجا باشند:
```
Caf → "Caf" با کاراکتر خراب
```

**علت:** کدگذاری اشتباه. کامپیوتر نمی‌تواند کاراکتر را بخواند.

**رفع:** اطمینان حاصل کنید که فایل با همان کدگذاری که انتظار دارید ذخیره و خوانده می‌شود.

### مشکل ۲: mojibake (مزبله متنی)

**علامت:** متن شبیه شمشیربازی:
```
Café (که باید باشد)
CafÃ© (که اگر UTF-8 به عنوان Latin-1 خوانده شود به نظر می‌رسد)
```

**علت:** ذخیره به عنوان UTF-8، اما خواندن به عنوان Latin-1.

**رفع:** از همان کدگذاری در هر دو جهت استفاده کنید!

### مشکل ۳: فقدان پشتیبانی از زبان

**علامت:** حروف فارسی/عربی معکوس یا جدا:
```
سلام → م ا ل س (حروف جدا یا معکوس)
```

**علت:** برنامه از رندر جهت راست به چپ (RTL) پشتیبانی نمی‌کند.

**رفع:** از ابزارهای با پشتیبانی Unicode و RTL استفاده کنید.

---

## کدگذاری در زبان‌های برنامه‌نویسی

### Python 3

```python
# رشته‌ها به طور پیش‌فرض یونیکد هستند
text = "سلام 😀"  # یونیکد ذاتی

# کدگذاری: رشته → بایت‌ها
encoded = "سلام".encode('utf-8')
# نتیجه: b'\xd8\xb3\xd9\x84\xd8\xa7\xd9\x85'

# رمزگشایی: بایت‌ها → رشته
decoded = b'\xd8\xb3\xd9\x84\xd8\xa7\xd9\x85'.decode('utf-8')
# نتیجه: "سلام"

# یافتن نقطه کد یونیکد
ord('A')    # نتیجه: ۶۵
ord('ا')    # نتیجه: ۱۵۷5

# ایجاد کاراکتر از نقطه کد
chr(65)     # نتیجه: 'A'
chr(1575)   # نتیجه: 'ا'
```

### JavaScript

```javascript
// رشته‌ها به طور داخلی UTF-16 هستند
let text = "سلام 😀";

// یافتن طول UTF-16 (نه کاراکترها!)
text.length;  // ۷ (😀 ۲ واحد کد UTF-16 است)

// نقطه کد یونیکد
text.charCodeAt(0);  // اولین واحد کد UTF-16

// از نقطه کد استفاده کنید
String.fromCharCode(65);     // "A"
String.fromCodePoint(128512); // "😀"
```

### Java

```java
// رشته‌ها به طور داخلی UTF-16 هستند
String text = "سلام";

// به بایت‌ها کدگذاری کنید
byte[] bytes = text.getBytes(StandardCharsets.UTF_8);

// از بایت‌ها رمزگشایی کنید
String decoded = new String(bytes, StandardCharsets.UTF_8);

// نقطه کد
text.codePointAt(0);  // اولین نقطه کد یونیکد
```

---

## بهترین روش‌ها

### ۱. همیشه UTF-8 استفاده کنید (مگر دلیل خیلی خوبی داشته باشید)
```
✓ UTF-8: استاندارد وب، سازگار، کارآمد
✗ Latin-1، Windows-1252: محدود، قدیمی
✗ UTF-16 برای ذخیره‌سازی: هدررفت برای متن انگلیسی
```

### ۲. همیشه کدگذاری را مشخص کنید
```python
# خوب: صریح باشید
with open('file.txt', 'w', encoding='utf-8') as f:
    f.write(text)

# اجتناب: کدگذاری پیش‌فرض سیستم
with open('file.txt', 'w') as f:  # می‌تواند متفاوت باشد!
    f.write(text)
```

### ۳. در هدرهای HTTP/FTP کدگذاری را اعلام کنید
```
Content-Type: text/html; charset=utf-8
```

### ۴. در اسناد HTML کدگذاری را اعلام کنید
```html
<meta charset="UTF-8">
```

### ۵. از BOM اجتناب کنید در UTF-8 (مگر نیاز باشد)
UTF-8 BOM معمولاً غیرضروری و گاهی مضر است.

---

## تمرین‌های عملی

### تمرین ۱: درک ASCII
این کدها را به کاراکتر تبدیل کنید:

۱. ۶۵ = _______
۲. ۴۸ = _______
۳. ۳۲ = _______
۴. ۹۰ = _______

### تمرین ۲: کدگذاری و رمزگشایی

**سناریو:** شما یک پیام دارید: "سلام"

۱. اگر به UTF-8 کدگذاری شود، چند بایت خواهد بود؟
۲. اگر به UTF-16 کدگذاری شود، چند بایت خواهد بود؟
۳. چرا UTF-8 برای فارسی کارآمدتر است؟

### تمرین ۳: شناسایی مشکلات کدگذاری

کدام مشکل کدگذاری ممکن است باعث موارد زیر شود؟

۱. "CafÃ©" به جای "Café" ظاهر می‌شود
۲. حروف فارسی به صورت جدا یا معکوس نمایش داده می‌شوند
۳. علائم سوال (؟؟؟) در متن ظاهر می‌شوند

### تمرین ۴: تمرین کدگذاری

Python را باز کنید (یا از یک مترجم آنلاین استفاده کنید) و امتحان کنید:

```python
# امتحان کنید
message = "Hello, 世界! 🌍"
print(message)
print(len(message))
print(message.encode('utf-8'))
```

تعداد بایت‌های هر قسمت را بشمارید. چرا اینقدر بیشتر است؟

---

## نکات کلیدی

۱. **کدگذاری کاراکتر = نقشه‌برداری** بین حروف و اعداد
۲. **ASCII = پایه**: ۱۲۸ کاراکتر، فقط انگلیسی
۳. **یونیکد = همه زبان‌ها**: بیش از ۱۴۳,۰۰۰ کاراکتر، یک فضای کد واحد
۴. **UTF-8 = پیاده‌سازی**: ۱-۴ بایت در هر کاراکتر، وب استاندارد
۵. **همیشه UTF-8 استفاده کنید**: مگر دلیل خیلی خوبی داشته باشید
۶. **همیشه کدگذاری را مشخص کنید**: پیش‌فرض‌های سیستم شما را اذیت می‌کنند

## به یاد داشته باشید

| کدگذاری | بایت‌ها در هر کاراکتر | زبان‌ها | استفاده در |
|---------|---------------------|--------|-----------|
| ASCII | ۱ | انگلیسی | همه جا (سازگار) |
| UTF-8 | ۱-۴ | همه | وب، مدرن |
| UTF-16 | ۲ یا ۴ | همه | ویندوز، جاوا |
| UTF-32 | ۴ | همه | نادر (پردازش) |

---

## گام‌های بعدی

- تمرین با کدگذاری/رمزگشایی در زبان برنامه‌نویسی خود
- درباره رندر متن و موتورهای چیدمان بیاموزید
- بررسی پشتیبانی از متن بین‌المللی در ابزارهای خود
- ببینید "الگوریتم کدگذاری کاراکتر" چگونه کار می‌کند
