# اصول فلوچارت: طراحی بصری الگوریتم

## فلوچارت چیست؟

فلوچارت نمایش بصری یک الگوریتم با استفاده از نمادهای استاندارد است که با فلش به هم متصل شده‌اند. جریان اجرا از شروع تا پایان را نشان می‌دهد و درک منطق در یک نگاه را آسان می‌کند.

### چرا از فلوچارت استفاده کنیم؟

- **وضوح بصری**: ساختار الگوریتم را فوراً ببینید
- **راحتی دنبال کردن**: با فلش‌ها جریان اجرا را دنبال کنید
- **شناسایی سریع خطاها**: نمایش بصری نقص‌های منطقی را آشکار می‌کند
- **زبان جهانی**: نمادها در سراسر جهان فهمیده می‌شوند
- **ابزار برنامه‌ریزی**: قبل از نوشتن کد طراحی کنید

---

## نمادهای فلوچارت

| نماد | نام | کاربرد |
|--------|------|---------|
| ⬭ | بیضی (پایانه) | نقاط شروع و پایان |
| ⬜ | مستطیل (پردازش) | اقدامات، محاسبات، انتساب‌ها |
| 🔷 | لوزی (تصمیم) | سوالات بله/خیر، شرایط |
| 🗄️ | متوازی‌الاضلاع (ورودی/خروجی) | خواندن ورودی یا نمایش خروجی |
| ➡️ | فلش (خط جریان) | جهت اجرا |
| ○ | دایره (اتصال‌دهنده) | پیوند به بخش دیگر فلوچارت |

---

## الگوریتم ۱: جمع کردن لیستی از اعداد

### مسئله
با داشتن لیستی از اعداد، مجموع آنها را محاسبه کنید.

### تفکر گام‌به‌گام
۱. شروع برنامه
۲. دریافت لیست اعداد
۳. شروع با sum = ۰ (هنوز چیزی اضافه نکرده‌ایم)
۴. نگاه کردن به هر عدد یکی‌یکی
۵. اضافه کردن هر عدد به مجموع در حال اجرا
۶. وقتی همه اعداد پردازش شدند، نمایش نتیجه
۷. پایان برنامه

### فلوچارت (نمایش ASCII)

```
                    +-------------+
                    |    شروع    |
                    +------+------+
                           |
                           v
                    +-------------+
                    |  خواندن لیست |
                    |   اعداد     |
                    +------+------+
                           |
                           v
                    +-------------+
                    | sum = ۰ تنظیم |
                    +------+------+
                           |
                           v
                    +-------------+
                    | شاخص = ۰    |
                    | تنظیم       |
                    +------+------+
                           |
                           v
              +----------------------+
              |  آیا اعداد دیگری   |----خیر---->
              |  برای بررسی exist؟   |          |
              +----------+-----------+          |
                         | بله                 |
                         v                      |
                  +-------------+                |
                  | افزودن عدد  |                |
                  | فعلی به     |                |
                  |    sum      |                |
                  +------+------+                |
                         |                       |
                         v                       |
                  +-------------+                |
                  | رفتن به     |                |
                  | عدد بعدی    |                |
                  +------+------+                |
                         |                       |
                         +-----------------------+
                                                 |
                                                 v
                                          +-------------+
                                          |  نمایش    |
                                          |  sum      |
                                          +------+------+
                                                 |
                                                 v
                                          +-------------+
                                          |    پایان   |
                                          +-------------+
```

### شبه‌کد
```
الگوریتم SumNumbers
    // محاسبه مجموع لیستی از اعداد
    
    DECLARE numbers AS ARRAY OF INTEGER
    DECLARE sum, i AS INTEGER
    
    sum ← ۰
    
    FOR each number IN numbers DO
        sum ← sum + number
    END FOR
    
    WRITE "Sum is: " + sum
END ALگوریتم
```

### راه‌رفتن نمونه
**ورودی**: [۵، ۳، ۸، ۲]

| گام | اقدام | مقدار sum |
|------|--------|-----------|
| ۱ | شروع با sum = ۰ | ۰ |
| ۲ | افزودن ۵ | ۵ |
| ۳ | افزودن ۳ | ۸ |
| ۴ | افزودن ۸ | ۱۶ |
| ۵ | افزودن ۲ | ۱۸ |
| ۶ | نمایش نتیجه | ۱۸ |

**خروجی**: ۱۸

---

## الگوریتم ۲: یافتن بزرگ‌ترین عدد

### مسئله
با داشتن لیستی از اعداد، بزرگ‌ترین را پیدا کنید.

### تفکر گام‌به‌گام
۱. شروع برنامه
۲. دریافت لیست اعداد
۳. فرض کنید اولین عدد maximum است (بزرگ‌ترینی که تا کنون دیده‌ایم)
۴. نگاه کردن به هر عدد باقی‌مانده
۵. اگر عددی بزرگ‌تر از maximum فعلی باشد، maximum را به‌روزرسانی کنید
۶. وقتی همه اعداد بررسی شدند، maximum را نمایش دهید
۷. پایان برنامه

### فلوچارت (نمایش ASCII)

```
                    +-------------+
                    |    شروع    |
                    +------+------+
                           |
                           v
                    +-------------+
                    |  خواندن لیست |
                    |   اعداد     |
                    +------+------+
                           |
                           v
                    +-------------+
                    | max = اولین |
                    | عدد تنظیم   |
                    +------+------+
                           |
                           v
                    +-------------+
                    | شاخص = ۱    |
                    | تنظیم       |
                    +------+------+
                           |
                           v
              +----------------------+
              |  آیا اعداد دیگری   |----خیر---->
              |  برای بررسی exist؟   |          |
              +----------+-----------+          |
                         | بله                 |
                         v                      |
              +----------------------+          |
              | آیا عدد فعلی        |            |
              | > max؟               |---خیر----->|
              +----------+-----------+            |
                         | بله                  |
                         v                      |
                  +-------------+               |
                  | به‌روزرسانی   |               |
                  | max به عدد    |               |
                  |   فعلی       |               |
                  +------+------+               |
                         |                      |
                         +----------------------
                                                 |
                                                 v
                                          +-------------+
                                          |  نمایش    |
                                          |    max    |
                                          +------+------+
                                                 |
                                                 v
                                          +-------------+
                                          |    پایان   |
                                          +-------------+
```

### شبه‌کد
```
الگوریتم FindMaximum
    // یافتن بزرگ‌ترین عدد در لیست
    
    DECLARE numbers AS ARRAY OF INTEGER
    DECLARE max, i AS INTEGER
    
    max ← numbers[۰]  // فرض کنید اولی بزرگ‌ترین است
    
    FOR i FROM 1 TO LENGTH(numbers) - 1 DO
        IF numbers[i] > max THEN
            max ← numbers[i]  // یکی بزرگ‌تر پیدا شد!
        END IF
    END FOR
    
    WRITE "Maximum is: " + max
END الگوریتم
```

### راه‌رفتن نمونه
**ورودی**: [۱۲، ۵، ۲۷، ۸، ۱۵]

| گام | اقدام | مقدار max |
|------|--------|-----------|
| ۱ | شروع با max = ۱۲ | ۱۲ |
| ۲ | ۵ > ۱۲؟ نه، نگه داشتن ۱۲ | ۱۲ |
| ۳ | ۲۷ > ۱۲؟ بله! به‌روزرسانی | ۲۷ |
| ۴ | ۸ > ۲۷؟ نه، نگه داشتن ۲۷ | ۲۷ |
| ۵ | ۱۵ > ۲۷؟ نه، نگه داشتن ۲۷ | ۲۷ |
| ۶ | نمایش نتیجه | ۲۷ |

**خروجی**: ۲۷

---

## الگوریتم ۳: یافتن کوچک‌ترین عدد

### مسئله
با داشتن لیستی از اعداد، کوچک‌ترین را پیدا کنید.

### تفکر گام‌به‌گام
۱. شروع برنامه
۲. دریافت لیست اعداد
۳. فرض کنید اولین عدد minimum است (کوچک‌ترینی که تا کنون دیده‌ایم)
۴. نگاه کردن به هر عدد باقی‌مانده
۵. اگر عددی کوچک‌تر از minimum فعلی باشد، minimum را به‌روزرسانی کنید
۶. وقتی همه اعداد بررسی شدند، minimum را نمایش دهید
۷. پایان برنامه

### فلوچارت (نمایش ASCII)

```
                    +-------------+
                    |    شروع    |
                    +------+------+
                           |
                           v
                    +-------------+
                    |  خواندن لیست |
                    |   اعداد     |
                    +------+------+
                           |
                           v
                    +-------------+
                    | min = اولین |
                    | عدد تنظیم   |
                    +------+------+
                           |
                           v
                    +-------------+
                    | شاخص = ۱    |
                    | تنظیم       |
                    +------+------+
                           |
                           v
              +----------------------+
              |  آیا اعداد دیگری   |----خیر---->
              |  برای بررسی exist؟   |          |
              +----------+-----------+          |
                         | بله                 |
                         v                      |
              +----------------------+          |
              | آیا عدد فعلی        |            |
              | < min؟               |---خیر----->|
              +----------+-----------+            |
                         | بله                  |
                         v                      |
                  +-------------+               |
                  | به‌روزرسانی   |               |
                  | min به عدد    |               |
                  |   فعلی       |               |
                  +------+------+               |
                         |                      |
                         +----------------------
                                                 |
                                                 v
                                          +-------------+
                                          |  نمایش    |
                                          |    min    |
                                          +------+------+
                                                 |
                                                 v
                                          +-------------+
                                          |    پایان   |
                                          +-------------+
```

### شبه‌کد
```
الگوریتم FindMinimum
    // یافتن کوچک‌ترین عدد در لیست
    
    DECLARE numbers AS ARRAY OF INTEGER
    DECLARE min, i AS INTEGER
    
    min ← numbers[۰]  // فرض کنید اولی کوچک‌ترین است
    
    FOR i FROM 1 TO LENGTH(numbers) - 1 DO
        IF numbers[i] < min THEN
            min ← numbers[i]  // یکی کوچک‌تر پیدا شد!
        END IF
    END FOR
    
    WRITE "Minimum is: " + min
END الگوریتم
```

### راه‌رفتن نمونه
**ورودی**: [۳۴، ۱۲، ۵۶، ۸، ۲۳]

| گام | اقدام | مقدار min |
|------|--------|-----------|
| ۱ | شروع با min = ۳۴ | ۳۴ |
| ۲ | ۱۲ < ۳۴؟ بله! به‌روزرسانی | ۱۲ |
| ۳ | ۵۶ < ۱۲؟ نه، نگه داشتن ۱۲ | ۱۲ |
| ۴ | ۸ < ۱۲؟ بله! به‌روزرسانی | ۸ |
| ۵ | ۲۳ < ۸؟ نه، نگه داشتن ۸ | ۸ |
| ۶ | نمایش نتیجه | ۸ |

**خروجی**: ۸

---

## الگوریتم ۴: مرتب‌سازی ساده (حبابی)

### مسئله
با داشتن لیستی از اعداد، آنها را از کوچک به بزرگ مرتب کنید.

### ایده
اعداد مجاور را مقایسه کنید. اگر در ترتیب اشتباه باشند، جابجا کنید. تا وقتی که هیچ جابجایی در یک عبور کامل اتفاق نیفتد، تکرار کنید.

### تفکر گام‌به‌گام
۱. شروع برنامه
۲. دریافت لیست اعداد
۳. نگاه کردن به هر جفت اعداد مجاور
۴. اگر یک جفت در ترتیب اشتباه باشد (بزرگ‌تر اول)، جابجا کنید
۵. تکرار فرآیند تا زمانی که هیچ جابجایی در یک عبور کامل اتفاق نیفتد
۶. لیست الآن مرتب شده است
۷. نمایش لیست مرتب شده
۸. پایان برنامه

### فلوچارت (نمایش ASCII)

```
                    +-------------+
                    |    شروع    |
                    +------+------+
                           |
                           v
                    +-------------+
                    |  خواندن لیست |
                    |   اعداد     |
                    +------+------+
                           |
                           v
              +----------------------+
              |  swapped = true    |
              +----------+-----------+
                         |
                         v
              +----------------------+
              | swapped = true؟      |----خیر---->
              +----------+-----------+          |
                         | بله                 |
                         v                     |
              +----------------------+         |
              | swapped = false    |         |
              +----------+-----------+         |
                         |                    |
                         v                    |
              +----------------------+         |
              | شاخص = ۰ تنظیم    |         |
              +----------+-----------+         |
                         |                    |
                         v                    |
    +--------------------+-----------------+   |
    | شاخص < طول - ۱؟                    |   |
    +----------+-------------+-------------+   |
               | خیر            | بله       |
               |                 v           |
               |    +--------------------+  |
               |    | آیا numbers[شاخص]  |  |
               |    | > numbers[شاخص+۱]؟ |  |
               |    +--------+-----------+  |
               |              | بله  | خیر   |
               |              v       |      |
               |    +-------------+  |      |
               |    | جابجایی دو   |  |      |
               |    |  عدد         |  |      |
               |    |             |  |      |
               |    | swapped =   |  |      |
               |    |   true تنظیم |  |      |
               |    +------+------+  |      |
               |           |         |      |
               |           +---------+      |
               |                 |         |
               |                 v         |
               |    +----------------+    |
               |    | شاخص = شاخص + ۱|    |
               |    +--------+-----------+   |
               |              |             |
               +--------------+             |
                                            |
                                            v
                                     +-------------+
                                     |  نمایش    |
                                     |  لیست     |
                                     |  مرتب شده |
                                     +------+------+
                                            |
                                            v
                                     +-------------+
                                     |    پایان   |
                                     +-------------+
```

### شبه‌کد
```
الگوریتم SimpleSort
    // مرتب‌سازی اعداد از کوچک به بزرگ
    
    DECLARE numbers AS ARRAY OF INTEGER
    DECLARE i, temp AS INTEGER
    DECLARE swapped AS BOOLEAN
    DECLARE n AS INTEGER
    
    n ← LENGTH(numbers)
    swapped ← TRUE
    
    WHILE swapped = TRUE DO
        swapped ← FALSE
        
        FOR i FROM 0 TO n-2 DO
            IF numbers[i] > numbers[i+1] THEN
                // جابجایی دو عدد
                temp ← numbers[i]
                numbers[i] ← numbers[i+1]
                numbers[i+1] ← temp
                swapped ← TRUE
            END IF
        END FOR
    END WHILE
    
    WRITE "Sorted list: " + numbers
END الگوریتم
```

### راه‌رفتن نمونه
**ورودی**: [۵، ۲، ۸، ۱]

**عبور ۱:**
- مقایسه ۵ و ۲: ۵ > ۲؟ بله! جابجا → [۲، ۵، ۸، ۱]
- مقایسه ۵ و ۸: ۵ > ۸؟ نه → [۲، ۵، ۸، ۱]
- مقایسه ۸ و ۱: ۸ > ۱؟ بله! جابجا → [۲، ۵، ۱، ۸]

**عبور ۲:**
- مقایسه ۲ و ۵: ۲ > ۵؟ نه → [۲، ۵، ۱، ۸]
- مقایسه ۵ و ۱: ۵ > ۱؟ بله! جابجا → [۲، ۱، ۵، ۸]
- مقایسه ۵ و ۸: ۵ > ۸؟ نه → [۲، ۱، ۵، ۸]

**عبور ۳:**
- مقایسه ۲ و ۱: ۲ > ۱؟ بله! جابجا → [۱، ۲، ۵، ۸]
- مقایسه ۲ و ۵: ۲ > ۵؟ نه → [۱، ۲، ۵، ۸]
- مقایسه ۵ و ۸: ۵ > ۸؟ نه → [۱، ۲، ۵، ۸]

**عبور ۴:**
- هیچ جابجایی! لیست مرتب شده است.

**خروجی**: [۱، ۲، ۵، ۸]

---

## بهترین شیوه‌های فلوچارت

### ۱. ساده نگه دارید
- یک فلوچارت برای هر الگوریتم
- برچسب‌های واضح و ساده
- اجتناب از شلوغی

### ۲. نمادهای استاندارد
- استفاده مداوم از نمادهای صحیح
- اختراع شکل‌های جدید نکنید
- پیروی از قرارداد

### ۳. جهت جریان
- معمولاً از بالا به پایین
- چپ به راست برای شاخه‌ها
- جهت‌های واضح فلش

### ۴. لوزی‌های تصمیم
- همیشه دو خروجی (بله/خیر، درست/نادرست)
- خروجی‌ها را واضح برچسب‌گذاری کنید
- فقط یک سوال در هر لوزی

### ۵. آزمایش فلوچارت شما
- با داده نمونه دنبال کنید
- همه مسیرها (شاخه‌های بله و خیر) را بررسی کنید
- صحت خروجی را تأیید کنید

---

## اشتباهات رایج برای اجتناب

| اشتباه | چرا اشتباه است | چگونه رفع کنیم |
|---------|---------------|------------|
| گمشده شروع/پایان | هر الگوریتم به آنها نیاز دارد | همیشه بیضی در هر دو طرف داشته باشید |
| فلش‌های نامشخص | جهت جریان ناشناخته است | فلش‌ها را برچسب‌گذاری کنید یا مسیرهای واضح استفاده کنید |
| تصمیمات گمشده | خطاهای منطقی | لوزی برای همه شرایط اضافه کنید |
| زیادی در یک جعبه | گیج‌کننده می‌شود | به گام‌های کوچک‌تر تقسیم کنید |
| بدون ورودی/خروجی | نمی‌دانیم چه چیزی وارد/خارج می‌شود | از متوازی‌الاضلاع برای I/O استفاده کنید |

---

## تمرین‌های عملی

### تمرین ۱: شمارش اعداد
ایجاد فلوچارتی که تعداد اعداد در لیست را بشمارد.

**راهنما**: مشابه جمع، اما یک شمارنده را افزایش دهید به جای اضافه کردن.

### تمرین ۲: یافتن میانگین
ایجاد فلوچارتی که میانگین لیستی از اعداد را محاسبه کند.

**راهنما**: ابتدا اعداد را جمع کنید، سپس تقسیم بر تعداد کنید.

### تمرین ۳: بررسی وجود عدد
ایجاد فلوچارتی که بررسی کند آیا عدد خاصی در لیست وجود دارد.

**راهنما**: اگر پیدا کردید زودتر متوقف شوید (این جستجوی خطی نامیده می‌شود).

---

## از فلوچارت به کد

### فلوچارت → شبه‌کد → Python

**گام فلوچارت**: مستطیل "sum = ۰ تنظیم"

**شبه‌کد**: `sum ← ۰`

**Python**: `sum = ۰`

**گام فلوچارت**: لوزی "آیا عدد > max؟"

**شبه‌کد**: 
```
IF number > max THEN
    max ← number
END IF
```

**Python**:
```python
if number > max:
    max = number
```

---

## خلاصه

### نکات کلیدی

1. **فلوچارت‌ها الگوریتم‌های بصری هستند**: تصاویری که منطق برنامه را نشان می‌دهند
2. **نمادهای استاندارد**: بیضی، مستطیل، لوزی، متوازی‌الاضلاع
3. **سه الگوی پایه**:
   - **ترتیب**: گام‌ها به ترتیب (مستطیل پس از مستطیل)
   - **تصمیم**: شاخه‌های بله/خیر (لوزی با دو خروجی)
   - **حلقه**: تکرار گام‌ها (فلش برگشت به گام قبلی)
4. **همیشه دنبال کنید**: قبل از کدنویسی با داده نمونه آزمایش کنید
5. **فلوچارت → شبه‌کد → کد**: فرآیند طراحی سه گامی

### الگوهای الگوریتمی که یاد گرفتید

| الگوریتم | ایده اصلی |
|-----------|-----------|
| جمع | شروع در ۰، افزودن هر عدد |
| حداکثر | ردیابی بزرگ‌ترین دیده شده |
| حداقل | ردیابی کوچک‌ترین دیده شده |
| مرتب‌سازی | مقایسه همسایگان، جابجایی اگر ترتیب اشتباه است |

---

## کارت مرجع سریع

```
نمادها:
⬭  شروع / پایان
⬜  پردازش (کاری انجام بده)
🔷  تصمیم (سوال بله/خیر)
🗄️  ورودی / خروجی
➡️  جهت جریان

الگوی حلقه:
    ⬭
    |
    v
   ⬜ (مقداردهی اولیه)
    |
    v
   🔷 <--< (بررسی شرط)
   /  \
 بله    خیر
  |      |
  v      v
 ⬜      ⬜ (پایان)
  |      |
  +------+
  (برگشت به حلقه)

الگوی تصمیم:
       🔷
      /  \
   بله    خیر
    |      |
    v      v
   ⬜      ⬜
    |      |
    +------+
       |
       v
```

به خاطر بسپارید: **اول فکر کنید، دوم بکشید، سوم کد بزنید!**
